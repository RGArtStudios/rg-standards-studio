# Copy to apps/api/.env.local and fill in values

DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/rg_standards_studio"
JWT_SECRET="generate-with: openssl rand -hex 32"
PORT=3010
NODE_ENV=development
WEB_URL=http://localhost:3011

# GitHub — Personal Access Token with repo scope
# github.com/settings/tokens → Generate new token (classic) → check repo
GITHUB_TOKEN=ghp_REPLACE_ME
GITHUB_OWNER=RGArtStudios
GITHUB_PLATFORM_REPO=rg-platform

# Seed admin credentials — change immediately after first login
ADMIN_EMAIL=admin@rainiergardens.io
ADMIN_PASSWORD=ChangeMe123!

# Copy to apps/web/.env.local
NEXT_PUBLIC_API_URL=http://localhost:3010
